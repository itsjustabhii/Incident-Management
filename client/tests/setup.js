/**
 * @file Client test setup
 * @description Configures the Jest/jsdom test environment for React components.
 * Imports @testing-library/jest-dom for additional DOM matchers
 * (e.g., toBeInTheDocument, toHaveTextContent).
 */

import '@testing-library/jest-dom';
